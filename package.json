{
  "name": "collab-draw",
  "private": true,
  
  "scripts": {
    "build": "tsc -b ",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:generate": "cd packages/db && pnpm prisma generate && cd ../..",
    "start:web": "cd apps/excallidraw-fe && pnpm run start",
    "start:backend": "cd apps/http-backend && pnpm run start",
    "start:websocket": "cd apps/ws-backend && pnpm run start"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
